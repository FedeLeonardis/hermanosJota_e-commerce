# Puerto del servidor (Render lo asignará automáticamente)
PORT=5000

# URI de conexión a MongoDB
DB_URI=mongodb+srv://usuario:password@cluster.mongodb.net/nombre_base_datos

# Clave secreta para JWT (usa una clave fuerte y única en producción)
JWT_SECRET=tu_clave_super_secreta_y_segura_12345

# URL del frontend en producción (para CORS)
FRONTEND_URL=https://tu-app.netlify.app

# Entorno de ejecución
NODE_ENV=development
NODE_ENV=production
